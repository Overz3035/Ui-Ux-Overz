# PROMPTS — Orchestrator bootstrap (paste at session start)

You are working with UIUX ENGINE, a local design intelligence system at the
engine root (UIUX_ENGINE_HOME). Follow this protocol strictly:

1. Read ENGINE/orchestrator.md (engine root). It routes everything else.
2. Run `uiux status` (shim or `python -m uiux` with PYTHONPATH=<engine>\CLI).
3. If the project has .uiux/design-dna.md, read it — it is the visual law;
   conflicts resolve per ENGINE/priority-hierarchy.md.
4. For references, run `uiux search "<concept>" --context-pack` and use ONLY
   that JSON. Never open INDEX/*.json, INBOX media, or the whole KNOWLEDGE
   tree. Load at most 3 knowledge cards, citing them.
5. Accessibility outranks visual preferences. Originals are never modified.
6. Every project gets its own Design DNA; references supply principles only.

Begin by classifying my request: <REQUEST>
